@* @model IEnumerable<ProductBatchSummaryViewModel>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card shadow-lg rounded-4">
                <div class="card-header bg-primary text-white text-center">
                    <h3 class="mb-0">Product Inventory</h3>
                </div>
                <div class="card-body">
                    <table class="table table-bordered table-striped">
                        <thead class="bg-light">
                            <tr>
                                <th>Recipe Name</th>
                                <th>Product Type</th>
                                <th>Expiration Date</th>
                                <th>Cost per Unit (€)</th>
                                <th>Units in Stock</th>
                                <th>Total Units</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.RecipeName</td>
                                    <td>@item.ProductType</td>
                                    <td>@item.ExpirationDate.ToString("yyyy-MM-dd")</td>
                                    <td>@item.CostPerUnit.ToString("0.00") €</td>
                                    <td>@item.UnitsInStock / @item.TotalUnits</td>
                                    <td>@item.TotalUnits</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div> *@